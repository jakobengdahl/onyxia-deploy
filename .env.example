#########################################
# Required (fill these)
#########################################

# Your apex domain (no protocol, no trailing dot)
# Example: example.org
BASE_DOMAIN=example.org

# Email used for Let's Encrypt account (ACME)
ACME_EMAIL=you@example.org


#########################################
# Optional (fill only if billing is NOT
# auto-attached when new projects are created)
#########################################

# Billing account to attach to newly created projects
# Find via: gcloud beta billing accounts list
# BILLING_ACCOUNT_ID=XXXXXX-XXXXXX-XXXXXX


#########################################
# Optional overrides (sane defaults below)
#########################################

# GCP region for the Autopilot cluster
REGION=europe-north1

# GKE Autopilot cluster name
CLUSTER_NAME=onyxia-poc

# Namespace to deploy Onyxia into
NAMESPACE=default

# Onyxia Helm chart repo (use your fork if needed)
HELM_CHART_REPO=https://inseefrlab.github.io/helm-charts

# Catalog (Helm repo index). Keep the explicit index.yaml.
CATALOG_URL=https://YOUR-GH-USER.github.io/helm-charts-interactive-services/index.yaml

# Custom UI resources (Markdown pages, theme, etc.)
CUSTOM_RESOURCES_URL=https://raw.githubusercontent.com/YOUR-GH-USER/onyxia-ui-content/main

# NOTE:
# - Do NOT set PROJECT_ID to get ephemeral projects like: scb-onyxia-lab-<5digits>
# - If you DO set PROJECT_ID, scripts will reuse that project instead of creating a new one.
# PROJECT_ID=scb-onyxia-lab-12345
